# Development

when developing, use cargo to compile the project:
```
cargo build
```

if you are also messing with the web app, launch this in another terminal to compile the typescript files into browser-readable javascript:
```
npx tsc --watch
```
